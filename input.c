#include <stdio.h>
#include "input.h"

int inputScelta() {
    int valore;
    scanf("%d", &valore);
    return valore;
}

double inputCifre() {
    double valore;
    scanf("%lf", &valore);
    return valore;
}
